@model List<AMIProjectView.Models.MeterViewModel>
@{
    Layout = "_DashboardLayout";
    ViewData["Title"] = "My Meters";
    var total = (ViewBag.Total ?? 0);
    var page = (ViewBag.Page ?? 1);
    var pageSize = (ViewBag.PageSize ?? 10);
    int totalPages = (ViewBag.TotalPages ?? 1);
}

<style>
    /* small adjustments to match admin table style */
    .consumer-meter-card {
        max-width: 1200px;
        margin: 0 auto;
    }

        .consumer-meter-card table {
            font-size: 0.95rem;
        }
</style>

<div class="card p-3 consumer-meter-card">
    <div class="d-flex flex-wrap gap-2 align-items-center mb-3">
        <input id="filterSerial" class="form-control" style="max-width:260px" placeholder="Enter Serial No" />
        <select id="filterStatus" class="form-select" style="max-width:200px">
            <option value="">-- All Status --</option>
            <option>Active</option>
            <option>Inactive</option>
            <option>Decommissioned</option>
        </select>
        <select id="filterCategory" class="form-select" style="max-width:260px">
            <option value="">-- All Categories --</option>
            <option>Residential Tariff</option>
            <option>Commercial Tariff</option>
            <option>Factory Tariff</option>
        </select>
        <input id="filterDate" type="date" class="form-control" style="max-width:200px" />
        <div class="ms-auto d-flex gap-2">
            <a asp-action="CreateMeter" class="btn btn-success btn-sm"><i class="bi bi-plus-lg"></i> Add</a>
            <a asp-action="Index" class="btn btn-outline-secondary btn-sm"><i class="bi bi-house"></i> Home</a>
        </div>
    </div>

    <div class="table-responsive">
        <table id="metersTable" class="table table-hover align-middle">
            <thead>
                <tr>
                    <th>Meter Serial No</th>
                    <th>Org Unit ID</th>
                    <th>Manufacturer</th>
                    <th>Firmware</th>
                    <th>Category</th>
                    <th>Install Date</th>
                    <th>Status</th>
                    <th class="text-end">Actions</th>
                </tr>
            </thead>
            <tbody>
                @if (Model == null || !Model.Any())
                {
                    <tr><td colspan="8" class="text-center text-muted">No meters to display</td></tr>
                }
                else
                {
                    foreach (var m in Model)
                    {
                        <tr data-date="@(m.InstallDate == default ? "" : m.InstallDate.ToString("yyyy-MM-dd"))">
                            <td>@m.MeterSerialNo</td>
                            <td>@m.OrgUnitId</td>
                            <td>@m.Manufacturer</td>
                            <td>@m.Firmware</td>
                            <td>@m.Category</td>
                            <td>@(m.InstallDate == default ? "" : m.InstallDate.ToString("yyyy-MM-dd"))</td>
                            <td>
                                @if (string.Equals(m.Status, "Active", StringComparison.OrdinalIgnoreCase))
                                {
                                    <span class="badge bg-success">Active</span>
                                }
                                else
                                {
                                    <span class="badge bg-secondary">@m.Status</span>
                                }
                            </td>
                            <td class="text-end">
                                <a asp-action="EditMeter" asp-route-serial="@m.MeterSerialNo" class="btn btn-primary btn-sm"><i class="bi bi-pencil"></i> Edit</a>
                                <form asp-action="DeleteMeter" method="post" class="d-inline" onsubmit="return confirm('Decommission this meter?');">
                                    <input type="hidden" name="serial" value="@m.MeterSerialNo" />
                                    <button class="btn btn-danger btn-sm"><i class="bi bi-trash"></i> Delete</button>
                                </form>
                            </td>
                        </tr>
                    }
                }
            </tbody>
        </table>
    </div>

    <div class="d-flex justify-content-center mt-2">
        <nav aria-label="Meter paging">
            <ul class="pagination pagination-sm">
                <li class="page-item @(page <= 1 ? "disabled" : "")">
                    <a class="page-link" asp-action="Meters" asp-route-page="@(page - 1)" asp-route-pageSize="@pageSize">Previous</a>
                </li>

                @for (int i = 1; i <= totalPages; i++)
                {
                    <li class="page-item @(i == page ? "active" : "")">
                        <a class="page-link" asp-action="Meters" asp-route-page="@i" asp-route-pageSize="@pageSize">@i</a>
                    </li>
                }

                <li class="page-item @(page >= totalPages ? "disabled" : "")">
                    <a class="page-link" asp-action="Meters" asp-route-page="@(page + 1)" asp-route-pageSize="@pageSize">Next</a>
                </li>
            </ul>
        </nav>
    </div>
</div>

@section Scripts {
    <script>
        (function () {
            const rows = Array.from(document.querySelectorAll('#metersTable tbody tr'));
            const filterSerial = document.getElementById('filterSerial');
            const filterCategory = document.getElementById('filterCategory');
            const filterStatus = document.getElementById('filterStatus');
            const filterDate = document.getElementById('filterDate');

            function applyFilters() {
                const serial = (filterSerial.value || "").trim().toLowerCase();
                const cat = filterCategory.value;
                const status = filterStatus.value;
                const date = filterDate.value; // yyyy-MM-dd

                rows.forEach(r => {
                    if (r.cells.length < 2) return;

                    const sOk = !serial || (r.cells[0].innerText || '').toLowerCase().indexOf(serial) >= 0;
                    const cOk = !cat || (r.cells[4].innerText || '') === cat;
                    const stOk = !status || (r.cells[6].innerText || '') === status;
                    const dOk = !date || (r.getAttribute('data-date') === date || (r.cells[5].innerText || '') === date);

                    r.style.display = (sOk && cOk && stOk && dOk) ? '' : 'none';
                });
            }

            filterSerial.addEventListener('input', applyFilters);
            filterCategory.addEventListener('change', applyFilters);
            filterStatus.addEventListener('change', applyFilters);
            filterDate.addEventListener('change', applyFilters);

            applyFilters();
        })();
    </script>
}
